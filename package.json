{
  "name": "planet-a-backend",
  "version": "1.0.0",
  "description": "A backend to serve json to the planet-a dashboard",
  "main": "index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "dev": "ts-node-dev src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "social-dist0rtion-protocol",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "redis": "^2.8.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.6",
    "@types/express": "^4.17.1",
    "@types/node": "^12.7.2",
    "@types/redis": "^2.8.13",
    "ts-node-dev": "^1.0.0-pre.41",
    "tslint": "^5.18.0",
    "typescript": "^3.5.3"
  }
}
